{
  "name": "apirest-hono",
  "description": "API REST con Hono.js, JWT y SQLite",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.6",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}